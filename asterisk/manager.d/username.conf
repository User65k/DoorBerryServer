; Each user has a section labeled with the username
; so this is the section for the user named "mark"
[username]
secret = password
deny=0.0.0.0/0.0.0.0
permit=127.0.0.1/255.255.255.255
read = call,originate,dtmf
write = call,originate,dtmf
